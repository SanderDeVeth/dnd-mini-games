[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dnd-mini-games"
version = "0.1.0"
requires-python = ">=3.14.0"
dependencies = [
    "fastapi>=0.121.2",
    "mcp>=1.21.2",
    "uvicorn>=0.38.0",
]
